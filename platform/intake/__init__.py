"""Private source intake: admission records, inspection and de-identified derivatives.

This package is private to the platform. Administration of policies, provenance
registrations and reviewer grants happens only through the IAM-only
`intake.admin_handler` entry point (`source-admission/1`).
"""
