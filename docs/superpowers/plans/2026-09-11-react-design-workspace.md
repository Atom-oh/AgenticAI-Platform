# Historical release record: collaborative React workspace

- Date: 2026-09-11
- Recorded implementation baseline: `7325c72`
- Current requirements: `SPEC.md` §7-1
- Current interface: `platform/workspace/REACT_CONTRACT.md`
- Status: Implementation and dated release evidence; not a current operational attestation

## Recorded implementation

- Fifteen real platform React components with code/type/token hashes, trusted compiler
  settings, import/AST validation, actual TypeScript/production build and source/dist export.
- Private/project separation and current membership checks; immutable published product
  guidelines with persisted typed ontology; atomic metadata publication, discussions
  and changed-guideline impact tracking.
- `creative` one candidate; `guided` one baseline plus 2–5 independent variants; frozen
  criteria and same-source repair with actual browser evidence and explicit visual scope.
- Human approval bound to exact source, bundle, components, rules and evidence, with
  transactional stale-rule rejection. Release rebuilds approved source without AI,
  checks identical artifacts and compares the approved start screen within 2%.
- Private source/dist/site archives and reports. Registered GitHub/GitLab export
  adapters enforce feature branches, allowed paths, exact source, idempotency and
  conflict handling. Actual commit SHAs survive later status-read failures.
- Planning/discussion/development sidebars tied to product/page/round and role.

## Recorded verification

| Evidence | Scope |
|---|---|
| 784 Python and 41 UI tests | Historical automated integration run |
| Restricted container React path | Real build and browser execution |
| Two Major findings fixed | Approval race and lost Git commit record; independently reproduced |
| Backend UPDATE_COMPLETE | Reported AWS update at that date |
| HTML/CSS/SVG/PNG/JPG intake | Live synthetic uploads and original-download hash match |
| Project guidelines | Persisted synthetic ontology: 7 nodes, 8 edges |
| Astra guided baseline + two variants, Fable creative | Live generation passed in two rounds, including five behavior rules and required guidance screen |
| Source-image comparison | `visual: not-run`; no image baseline was supplied |
| Approved Astra/Fable releases | Same-source AWS rebuild/browser verification and 2% approved-start-screen comparison |
| Private archives | Hash-matched authenticated downloads; Astra source/dist 34,540/53,101 bytes, Fable 36,664/54,049 bytes |
| Published frontend | Recorded live API/project/preview/release check at FHD/QHD/WQHD; role coverage additionally tested offline |

The five-variant limit was covered by automated tests, not by the two-variant live run.
Source-image comparison and approved-start-screen comparison are different evidence.

## Integration and remaining boundaries

The earlier unchecked PR items are historical: the workspace was merged in PR #1,
and runnable pinned React samples followed in PR #2. Use the actual current HEAD for
new reviews; previous approval does not cover later changes.

No customer React package, configured customer Git destination, unsupplied financial
API/authentication/transaction integration, FIG interpretation, or bulk legacy-gallery
migration was certified. Local bare-Git tests do not prove customer remote writes.
Public platform-source authorization is not authorization to publish customer artifacts.
